import { ChangeFormatPipe } from './change-format.pipe';

describe('ChangeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
