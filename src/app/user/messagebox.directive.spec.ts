import { MessageboxDirective } from './messagebox.directive';

describe('MessageboxDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageboxDirective();
    expect(directive).toBeTruthy();
  });
});
